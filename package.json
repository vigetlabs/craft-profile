{
  "name": "craft-profile-average",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "short": "./bin/run -b http://lupus-resource-center.dev:3000 -p /,/topics,/collections -i 20 -l 10",
    "long": "./bin/run --baseUrl=http://lupus-resource-center.dev:3000 --paths=/,/topics,/collections --iterations=20 --parallel=7",
    "json": "./bin/run < config.json",
    "config": "./bin/run -c config.json",
    "configlong": "./bin/run --config config.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Solomon Hawk <solomon.hawk@viget.com> (http://viget.com/)",
  "license": "ISC",
  "dependencies": {
    "minimist": "1.2.0",
    "phantomjs-prebuilt": "2.1.8"
  }
}
